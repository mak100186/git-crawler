# Project Management: GitHub Hidden Gems Discovery Platform

> Status: ACTIVE
> Version: v33
> Last updated: 2026-08-04
> PRD: docs/prd.md (built against v8)
> Architecture: docs/architecture.md (built against v24)

## Phases & Milestones

| Phase | Goal | Status |
|-------|------|--------|
| Phase 0 | De-risk the two open feasibility questions (A1, A2) and stand up the deployable skeleton | Done |
| Phase 1 | Core data pipeline: ingest and score repositories | Done |
| Phase 2 | AI summarization and trend detection; dashboard UX design brief runs in parallel | Done |
| Phase 3 | Dashboard, API, and bookmarking | Done |
| Phase 4 | Daily digest and observability | Done |
| Phase 5 | Security, reliability, and scalability hardening | Planned |

## Feature Backlog

| ID | Feature | Phase | Priority | Status | Acceptance Criteria |
|----|---------|-------|----------|--------|---------------------|
| F-001 | Spike: GitHub GraphQL rate-limit budget validation | 0 | Must | Done | Point-cost model measured against a simulated 1K-5K repos/day discovery query; documented budget and back-off strategy; risk A1 marked resolved or a mitigation adopted. |
| F-002 | Spike: LM Studio inference throughput benchmark | 0 | Must | Done | Model identifier confirmed available in LM Studio's catalog — **confirmed 2026-08-01: `google/gemma-4-e4b` (spike §2 addendum)**; benchmark methodology executed live 2026-08-01 (spike §9.2), Pass vs. NFR-001 with ~10x headroom (spike §9.6); risk A2 resolved (spike §7/§9.5, Architecture §8). **Live run also found `gemma-4-e4b` truncates output on reasoning-token overhead (spike §9.4) — this led to a live comparison against 4 alternatives (spike §10) and a final model swap to `llama-3.2-3b-instruct` (ADR-017, supersedes ADR-013), which passes both throughput (0.78-1.05s mean, spike §10.2) and completeness (zero reasoning waste) checks.** |
| F-003 | Project scaffolding & Docker Compose skeleton | 0 | Must | Done | .NET 10 solution (all projects targeting net10.0), with Wolverine added and a vertical-slice folder convention established, and Angular 22 CLI project (standalone components; Angular Material + CDK added, verified compatible with Angular 22, and themed) both build; key .NET package dependencies (EF Core, Hangfire, Wolverine, GitHub API client, Npgsql) confirmed compatible with .NET 10 and PostgreSQL 18; Angular build output is copied into the ASP.NET Core host's static file root as part of the build/Docker image; Docker Compose brings up the app container (API + served dashboard) and `postgres:18.4` (pinned tag, not `latest`) together, health-checked; **LM Studio runs host-installed, not containerized (ADR-016 — amended 2026-08-01, operator already runs it natively) — a `Makefile` (`make up`) checks Docker, brings up Compose, checks/starts the host LM Studio server, and loads the configured model; `make status` confirms all three (app, postgres, LM Studio) are reachable.** |
| F-004 | Data Store schema (EF Core) | 1 | Must | Done | Migrations exist for repositories, scores, summaries, trend aggregates, bookmarks, and Hangfire job storage tables; applies cleanly to a fresh PostgreSQL 18.4 instance. **Hangfire's own storage tables are created by Hangfire.PostgreSql at F-006 runtime (its own `hangfire` schema), not by an EF Core migration — documented on `GitCrawlerDbContext` so F-006 doesn't duplicate schema setup.** |
| F-005 | GitHub Crawler | 1 | Must | Done | Implemented as a Wolverine command/handler slice (ADR-015); discovers new/updated repos via GraphQL (REST fallback for unsupported fields); writes raw metadata to the Data Store; respects the rate-limit budget from F-001 (FR-001). **§6/§7 mitigations from the F-001 spike genuinely implemented: rate-limit-aware retry (GraphQL `RATE_LIMITED`/`resetAt`, REST `x-ratelimit-*`/`Retry-After`, generic exponential backoff otherwise) and contributor-count caching (7-day freshness window) to keep the REST fallback — the actual binding constraint at scale — sustainable.** |
| F-006 | Job Scheduler (Hangfire) | 1 | Must | Done | Pipeline stages trigger on schedule via `RecurringJob` + `ContinueJobWith` chaining, each invoking the target stage's Wolverine command (ADR-015); a mid-run container restart resumes without duplicating or dropping work; dashboard reachable. **v1 has one real chain link (daily `RecurringJob` → Crawler via `DiscoverRepositoriesJob`); the `ContinueJobWith` attachment point for F-007 is documented in code, not stubbed. Dashboard is unauthenticated (operator decision, 2026-08-02) — a shared-secret query-string filter was tried first but broke the dashboard's own CSS/JS/stats requests (see ADR-009 Consequences), and no auth system exists elsewhere in this single-operator v1 to replace it with.** |
| F-007 | Scoring Engine | 1 | Must | Done | Implemented as a Wolverine command/handler slice (ADR-015); computes a hidden-gem score from license presence/type, commits-per-week, contributor count, fork count, and star count as independently identifiable, weighted inputs (star count added post-signoff per operator direction, weighted secondary to the primary four per Architecture §3's "existing popularity/quality signals"); scores persisted per repo (FR-002, FR-005). **Pipeline chain completed: `DiscoverRepositoriesJob` now attaches `ComputeScoresJob` via Hangfire `ContinueJobWith` after each crawl, so crawl → score runs end-to-end per Architecture §3's Job Scheduler dependency ordering.** |
| F-008 | Summarizer (LM Studio + Llama 3.2 3B Instruct) | 2 | Must | Done | Implemented as a Wolverine command/handler slice (ADR-015) calling `IRepositorySummarizer`, which calls LM Studio's local API running Llama 3.2 3B Instruct (ADR-017, supersedes ADR-013); generates a plain-text summary for top-scored repos without one; meets the throughput bar validated in F-002 (FR-003). **`Features/Summarization/GenerateSummaries/` — selects repos by latest (by `ComputedAtUtc`) `Score.TotalScore ≥ Summarization:MinimumScore` (default 40, judgment call) without an existing `Summary` row (create-once, never regenerated), capped at `Summarization:BatchSize` (default 200); README fetched via GitHub REST (`GET /repos/{owner}/{repo}/readme`, reusing the Crawler's REST client, 404 handled gracefully); per-repo failures (README or LM Studio) logged and skipped, not batch-aborting. `ComputeScoresJob` now attaches `GenerateSummariesJob` as chain link 3 via `ISummarizationContinuationLink`, completing the crawl → score → summarize chain. As of 2026-08-04: the prompt asks for a single plain-text passage (no headings/bullet points/section labels) of about 3 sentences capped at `Summarization:MaxSummaryLength` characters (default 180, new config) instead of a "concise, structured" multi-section summary — the length is enforced by asking the model for it, not by post-hoc truncation. `GenerateSummariesJob` also has its own standalone recurring trigger now (`Hangfire:SummarizationCronSchedule`, default hourly), on top of its existing chain attachment, so a scored-but-unsummarized backlog isn't stuck waiting for the next full daily crawl cycle. As of 2026-08-04 (later same day): generates two distinct summaries per repo, not one - `IRepositorySummarizer.SummarizeAsync` now returns a `RepositorySummaryResult(ShortSummary, DetailedSummary)`, `LmStudioRepositorySummarizer` makes two separate LM Studio calls (short capped at `Summarization:MaxSummaryLength`, detailed at the new `Summarization:MaxDetailedSummaryLength`, default 900), and `Summary.Content` is split into `Summary.ShortContent`/`Summary.DetailedContent` (migration `SplitSummaryContentIntoShortAndDetailed`, which deletes every pre-existing Summary row rather than leaving `DetailedContent` empty for them - operator-confirmed). `RepositoryCardDto`/`HiddenGemCardDto` gained `DetailedSummaryContent`; the dashboard card still shows the short one, the click-through detail dialog now shows the detailed one instead of the (previously shared) short one.** |
| F-009 | Trend Aggregator | 2 | Must | Done | Implemented as a Wolverine command/handler slice (ADR-015); rolls up scored + summarized repos into technology/framework/ecosystem trend summaries on schedule; persisted for the dashboard and digest to consume (FR-008). **`Features/Trends/AggregateTrends/` — category = `Repository.PrimaryLanguage` (null-language repos excluded); counts repos with both a `Score` and a `Summary`, using each repo's latest (by `ComputedAtUtc`) `TotalScore`; single-day period by default (`Trends:PeriodDays`, default 1); upsert-by-`(Category, PeriodStart, PeriodEnd)` — a third distinct persistence pattern alongside Score's append-history and Summary's create-once, needed for NFR-003 idempotency. `GenerateSummariesJob` now attaches `AggregateTrendsJob` as chain link 4 via `ITrendsContinuationLink`, completing crawl → score → summarize → aggregate trends end-to-end.** |
| F-010 | Web API | 3 | Must | Done | Endpoints organized as Wolverine command/query slices, one per operation (ADR-015); serve Discovery Feed, Hidden Gems, Trending, and Categories queries with filter/sort by language, star range, topic, and license; bookmark create/list/delete endpoints (FR-004, FR-007). **8 slices under `Features/{Repositories,Trends,Categories,Bookmarks}/` (GetDiscoveryFeed, GetHiddenGems, GetTrending, GetCategories, GetCategoryRepositories, CreateBookmark, DeleteBookmark, ListBookmarks), sharing one internal (non-Wolverine) filter/sort/paginate helper (`RepositoryCardQuery`) rather than tripling the D4 contract. Two additive schema columns bundled in (same precedent as F-007's star-count amendment): `Repository.Topics` (`text[]`, plus F-005's crawler extended to fetch `repositoryTopics` via GraphQL, capped 10/repo) — topics were never captured before this feature despite being in FR-004's scope; and `Repository.FirstDiscoveredAtUtc` (set once on first insert, drives the Discovery Feed's default "Newest" sort — `LastCrawledAtUtc` was the wrong field, since it advances on every re-crawl). "Categories" stayed `TrendAggregate.Category`-derived (i.e. `PrimaryLanguage`), not GitHub-topic-derived, since F-009 already shipped that semantic. Hidden Gems exposes the full FR-005 weighted signal breakdown (not just a total), using `ScoringWeights`' exact constants. Trending's contributing-repos list is computed at query time, mirroring `AggregateTrendsCommandHandler`'s membership criteria exactly (no repo-level FK on `TrendAggregate`, by design). Bookmark create/delete are idempotent (repeat calls never error). PASS on first Developer/Reviewer attempt; Integration PASS (121/121 tests, 0 vulnerabilities); Reviewer-Integration initially FAILed on a misattributed `docs/test-cases.md` diff (the Orchestrator, not Integration, authored the TC-010 scenarios pre-Integration per this pipeline's Step 0.0 — verified against the skill's own text and reversed to PASS). **Amendment (2026-08-03, operator-directed, not orchestrated): `GetCategoryRepositories` (the Category drill-down's dedicated endpoint) removed along with F-011's Categories tab below — `GetCategories` itself is unchanged and stays mapped, since it still backs the dashboard's Language filter option list (`FacetOptionsService`). 116/121 backend tests remain (the 5 removed were `GetCategoryRepositoriesQueryHandlerTests`, scoped entirely to the deleted endpoint).** **Second amendment (2026-08-03, same session, also operator-directed): `GetTrending` removed entirely (unlike `GetCategories`, nothing besides the now-removed Trending view ever consumed it) — `GetHiddenGemsQueryHandler` now computes each card's own `TrendGrowth` directly from `TrendAggregate` (same current/previous-period formula the old Trending view used client-side), added as a new field on `HiddenGemCardDto`. 114/121 backend tests remain net of both amendments (4 new `GetHiddenGemsQueryHandlerTests` cases added for `TrendGrowth`, the `GetTrendingQueryHandlerTests` file removed).** **Third amendment (2026-08-03, same session, also operator-directed): `GetDiscoveryFeed` removed entirely too — once Categories and Trending had already folded away, `GetHiddenGems` was left as the full-featured superset of the shared D4 filter/sort/paginate contract, so Discovery Feed offered no distinct API capability left to keep. 89/89 backend tests remain net of all three amendments (`GetDiscoveryFeedQueryHandlerTests`, 25 cases, removed).** **Fourth amendment (2026-08-03, same session, also operator-directed): `ListBookmarks` (the GET-list side of bookmarking; `CreateBookmark`/`DeleteBookmark` untouched) removed along with F-011/F-012's dedicated Bookmarks view below — nothing else consumed the list endpoint, same situation as `GetTrending`/`GetDiscoveryFeed` rather than `GetCategories`'s kept-because-still-used case. `RepositoryCardQuery.ToCardDto` (the bare-`RepositoryCardDto` factory `ListBookmarksQueryHandler` alone called) removed too, now genuinely dead. 86/86 backend tests remain net of all four amendments (`ListBookmarksQueryHandlerTests` removed).** |
| F-011 | Web Dashboard (Angular + Angular Material) | 3 | Must | Done | Discovery Feed, Hidden Gems, Trending, and Categories render as distinct views, built with Angular Material components, backed by F-010; filter/sort controls work end-to-end; Angular build served correctly as static assets from the ASP.NET Core host (FR-009). **Implemented as 5 standalone routed components (`features/{discovery-feed,hidden-gems,trending,categories,categories/category-detail}`) against the approved "Ink Header" visual design (`dashboard-handoff.md`); shared `RepositoryCard`/`RepositoryGrid`/`FilterSortBar`/`BookmarkToggle` components reused across views. Reviewer FAILed once (missing 960px filter-bar collapse to a "Filters · N" `mat-sidenav`; Categories tiles + mobile bottom-nav built from custom-styled anchors instead of `mat-card`/`mat-icon-button`, violating ADR-011) — both fixed and re-verified, PASS on round 2. F-010 has no facet-options endpoint (`/api/languages`\|`licenses`\|`topics`); language options are sourced from `/api/categories`, license/topic options accumulated client-side from repository cards already fetched that session — a pre-flagged, accepted contract gap, not a defect. The subsequent Integration pass found and fixed a genuine pre-ship runtime defect the Reviewer round hadn't caught: `FilterSortBar` was missing a `MatInputModule` import, so the Star-range Min/Max facet inputs would have thrown `mat-form-field must contain a MatFormFieldControl` in a live browser — "filter/sort controls work end-to-end" above was not actually true until that fix landed. **Amendment (2026-08-03, operator-directed, not orchestrated): the standalone Categories tab and its Category drill-down (`features/categories/`) were decommissioned — `features/discovery-feed`, `hidden-gems`, and `trending` are now the three dashboard views, plus F-012's `bookmarks`. Browsing by category is unaffected in substance: Category ≡ `Repository.PrimaryLanguage`, and `FilterSortBar`'s existing Language facet (already shown on Discovery Feed/Hidden Gems) already filters on that exact value — only the redundant dedicated tab was removed, along with the now-dead `forcedCategory` pinned-chip plumbing it alone required in `FilterSortBar`. Nav order is now Discovery Feed → Hidden Gems → Trending → Bookmarks (Bookmarks shifts from 5th to 4th). 51/51 frontend tests remain (10 removed: `categories.spec.ts`/`category-detail.spec.ts` deleted with their components, plus one `filter-sort-bar.spec.ts` case and one `query-params.util.spec.ts` case scoped to the removed `forcedCategory`/`omitLanguage` behavior).** **Second amendment (2026-08-03, same session, also operator-directed): the standalone Trending view (`features/trending/`) was likewise decommissioned and merged into Hidden Gems — `RepositoryCard` gained a new, independently-optional `trendGrowth` input (rendered as a chip, same visual language as the old `.trend-card__growth-chip`), wired through `RepositoryGrid` from `HiddenGemCardDto.trendGrowth`. Nav order is now Discovery Feed → Hidden Gems → Bookmarks (three entries total). Dead plumbing removed alongside: `TrendingApiService`, `trend.model.ts`, and the now-unused `trending-up` icon. 47/47 frontend tests remain (trending.spec.ts's tests removed with its component; one new `repository-card.spec.ts` case and one new `hidden-gems.spec.ts` case added for the trend-growth chip).** **Third amendment (2026-08-03, same session, also operator-directed): the standalone Discovery Feed view (`features/discovery-feed/`) was decommissioned too, per the operator's own read that it offered no meaningful difference from Hidden Gems once Categories/Trending had folded into it — the default route now lands on Hidden Gems, and the primary nav is just Hidden Gems → Bookmarks (two entries). `RepositoryCard`'s avatar-initial circle was also removed per the same feedback ("remove the avatar"), leaving the score badge as Hidden Gems' sole header visual anchor (Bookmarks cards, which carry no score breakdown, now render no header circle at all). 42/42 frontend tests remain (discovery-feed.spec.ts's 5 tests removed with its component; `app.spec.ts`/`app.routes.spec.ts` updated for the two-entry nav and Hidden-Gems-default route).** **Fourth amendment (2026-08-03, same session, also operator-directed): repo-card layout polish plus a new click-to-open repository detail pane (fulfilling the design brief's own §09 mockup, never previously implemented) — `.repo-card__summary` now clamps to 3 lines (was 2) with matching extra room in the "Summary pending" placeholder, and the footer chip row sits further below its divider line (`padding-top` 9px → 16px). Clicking anywhere on a card except its own interactive controls (bookmark toggle, "Why this score?" panel, "Open on GitHub" link — each stops click propagation) now opens a right-side `mat-drawer` (mode="over") showing the same repo's full untruncated summary, its topics (not shown on the compact card at all), and — when available — its always-expanded score breakdown; owned by `RepositoryGrid` since that's the one component every card-clicking view shares. New shared `shared/utils/score-breakdown.util.ts` extracted from `RepositoryCard`'s own scoring-display math so the compact card and the new detail pane can't drift apart on the five-signal breakdown. 51/51 frontend tests remain (6 new: 2 on `RepositoryCard` for click propagation, 2 on the new `RepositoryDetailPane`, 2 on `RepositoryGrid` for the drawer open/close wiring).** **Fifth amendment (2026-08-03, same session, also operator-directed): F-012's dedicated Bookmarks view (`features/bookmarks/`) was decommissioned — the operator's own read was that it was just a filter on Hidden Gems, which is accurate: `FilterSortBar`'s existing "Bookmarked only" toggle (`showBookmarkedToggle`, already `true` on Hidden Gems) surfaces the identical set of repos. The primary nav is now Hidden Gems alone (one entry); the default route is unchanged. 47/47 frontend tests remain (bookmarks.spec.ts's tests removed with its component).** |
| F-012 | Bookmarking | 3 | Must | Done | User can bookmark a repo from the dashboard and revisit it later from a dedicated bookmarks view; persisted via F-010 (FR-007). **Bookmark toggle + backend CRUD had already shipped inside F-010/F-011's own scope; this run's actual delta was the dedicated `/bookmarks` view (`features/bookmarks/`) and wiring the app shell's inert "Bookmarks · F-012" nav ghost into a live 5th nav entry. Un-bookmarking from this view removes the card from the grid (Undo restores it via the existing snack-bar) — implemented as a component-scoped DI override of `BookmarkApiService` (`skipSelf`-delegating, tapped for confirmed add/remove) so `BookmarkToggle`/`RepositoryCard`/`RepositoryGrid` stayed untouched; `GET /api/bookmarks` (F-010, unpaginated) is fed through the existing `RepositoryGrid` with an inert single-page paginator. PASS on first Developer/Reviewer attempt.** **Amendment (2026-08-03, operator-directed, not orchestrated): the dedicated `/bookmarks` view this feature's own delta consisted of was decommissioned — Hidden Gems' pre-existing "Bookmarked only" filter (`FilterSortBar.showBookmarkedToggle`) already surfaces the identical set of bookmarked repos, so the operator judged a separate view redundant. FR-007's "revisit it later" is still satisfied, now via that filter rather than a dedicated route; `ListBookmarksQueryHandler`/`GET /api/bookmarks` and the DI-override pattern above were removed along with the view (create/delete bookmark endpoints and the toggle itself are untouched — see F-010's own amendment below). This narrows F-012's remaining footprint to "bookmark toggle exists and persists," already covered by F-010/F-011's own original scope, but the row stays `Done` rather than reopened, matching this doc's established amendment-not-reopen convention for scope changes to already-shipped features.** |
| F-013 | Digest Service | 4 | Should | Done | Implemented as a Wolverine command/handler slice (ADR-015); composes and sends a daily email with top hidden gems and trend summaries via SMTP; failure to send is logged, not silently dropped (FR-006). **`Features/Digest/SendDigest/` — `SendDigestCommandHandler` ranks eligible (scored + summarized) repos by latest `Score.TotalScore` (same latest-by-`ComputedAtUtc` rule as every other stage), takes the top `Digest:TopN` (default 10) with each repo's `Summary.ShortContent`, and pulls the current period's `TrendAggregate` rows for the trend section. Sends via a new `IEmailSender`/`SmtpEmailSender` abstraction (BCL `SmtpClient`, no new package); a send failure is caught, logged, and returned as `Sent: false` — never propagates, never crashes the host. Deliberately **not** chained onto `AggregateTrendsJob` (link 5) — that job now fires roughly hourly (chained off `GenerateSummariesJob`'s own hourly schedule), so chaining would email hourly instead of daily. Gets its own independent `RecurringJob` instead (`Hangfire:DigestCronSchedule`, default `0 6 * * *`, after the 03:00 crawl-chain default has had time to complete). PASS on first Developer/Reviewer attempt; Integration PASS (103/103 backend tests).** |
| F-014 | Observability | 4 | Should | Done | Structured logging/metrics implemented as a Wolverine middleware (ADR-015) wrapping every command/query platform-wide, emitting records processed, duration, and failures per stage — beyond what the Hangfire dashboard (F-006) already covers — enough to diagnose a stuck or rate-limited run without a debugger (NFR-005). **New `Infrastructure/Observability/` folder — the first platform-wide, non-feature-scoped code in this codebase (legitimate exception to ADR-015's vertical-slice convention, since this is cross-cutting by nature). Two complementary Wolverine idioms, both registered globally in `Program.cs`'s `UseWolverine(opts => ...)` block (not opted into individual handlers): `ObservabilityMiddleware` (attribute-based `[WolverineBefore]`/`[WolverineOnException]`) logs a "Starting"/"Completed" pair with duration and success/failure status per invocation; `RecordsProcessedPolicy` (a raw `IChainPolicy`) supplies the records-processed count via a documented priority heuristic (named `*Count` property → collection count → default `1` — deliberately no "any int/long property" fallback, since that misreported entity-identifier fields like `BookmarkDto.Id` as a fake count, caught and fixed in Reviewer round 1). A failed invocation logs `RecordsProcessed=0` (documented as "failed before producing a countable result") — also a round-1 Reviewer fix. Adds detail beyond Hangfire's job-level dashboard (start/end/succeeded/failed/retry-count): per-invocation records-processed/duration for every command and query, not just scheduled jobs. Developer/Reviewer loop PASSed after 1 retry (2 iterations); Integration PASS (103/103 backend tests, one test-only bug fixed — a comparison against the wrong `Envelope.MessageType` string shape, not a production defect).** |
| F-015 | Security hardening | 5 | Must | Planned | GitHub API token loaded from environment/secrets config, never logged or committed; a repo-wide secret-scan check passes (NFR-002). |
| F-016 | Reliability/idempotency pass | 5 | Must | Planned | Every pipeline stage re-run against partially-completed state produces no duplicate records; GitHub API failures retry with backoff instead of aborting the run (NFR-003). |
| F-017 | Scalability: indexing & partitioning strategy | 5 | Should | Planned | Query plans for the dashboard's core filter/sort paths remain performant against a seeded dataset sized toward the 100k+ repos / 1M+ records target (NFR-004). |
| F-018 | Dashboard UX design brief & Claude Designer handoff | 2 | Must | Done | A written design brief (authored by Claude) covers: the Discovery Feed, Hidden Gems, Trending, and Categories layouts; filter/sort and bookmark interactions (FR-004, FR-007, FR-009); and an explicit constraint that all UI is composed from Angular Material components (ADR-011) with no custom/non-Material widgets. Brief is handed to Claude Designer; the resulting UX design is reviewed and approved before F-011 implementation begins. **`docs/design-briefs/dashboard-ux-brief.md` — bookmark "list" interaction (FR-007) resolved as a "bookmarked only" filter toggle within the four required views rather than a fifth dedicated view (that's F-012's scope); three genuine Material component gaps (infinite scroll, trend sparkline, skeleton loader) flagged explicitly with Material-native fallbacks. Handoff to Claude Designer is a document handoff, not a tool invocation. **The design pass has since run and been reviewed/approved (2026-08-02, prior to the F-010 session)**: `docs/design-briefs/dashboard-design-directions-exploration/project/Dashboard Design.dc.html` (direction "Ink Header," deep-olive second accent) + `.../handoff/dashboard-handoff.md` are the approved build target — three gaps raised in the first review round (filter-panel opened-state internals, the "Summary pending" placeholder treatment, and explicit sign-off on three brief-delegated choices: star range = slider+inputs both, sort = `mat-button-toggle-group`, filter bar = persistent pill row) were all resolved in a revision and confirmed resolved on re-review. F-018's gating condition on F-011 is now satisfied — F-011 is implementation-ready. |

(IDs must be stable — never renumber once assigned)

## Dependencies

| Item | Depends On | Blocks | Notes |
|------|-----------|--------|-------|
| F-004 | F-003 | F-005, F-006, F-007 | Schema needs the scaffolded solution/containers first |
| F-005 | F-001, F-004 | F-007 | Crawler design should reflect the validated rate-limit budget |
| F-006 | F-004 | F-016 | Hangfire persistent storage lives in the Data Store schema |
| F-007 | F-004, F-005 | F-008, F-009, F-013 | Scoring needs raw metadata already ingested |
| F-008 | F-002, F-004, F-007 | F-009 | Summarizer targets the throughput bar validated in F-002; summarizes top-scored repos |
| F-009 | F-007, F-008 | F-010, F-013 | Trend rollup reads scored + summarized repos |
| F-010 | F-004, F-007, F-008, F-009 | F-011, F-012 | API surfaces the full pipeline's output |
| F-011 | F-010, F-018 | F-012 | Dashboard is built against both the API and the approved UX design |
| F-018 | — | F-011 | Can proceed independently of backend work — needs only the approved Functional Requirements (Architecture §5), not a running API |
| F-012 | F-010, F-011 | — | Bookmarking spans both the API and dashboard |
| F-013 | F-007, F-009 | — | Digest needs top gems + trend aggregates |
| F-014 | F-005, F-006, F-007, F-008, F-009 | — | Instrumented once the stages it observes exist; can proceed incrementally alongside them |
| F-015 | F-003 | — | Hardens the token/secrets handling introduced in scaffolding |
| F-016 | F-005, F-006, F-007, F-008, F-009 | — | Idempotency pass across all pipeline stages |
| F-017 | F-004 | — | Indexing strategy applied to the schema from F-004 |

## Out of Scope (confirmed in PRD)

**Deferred future enhancements:**
- Personalized discovery via user-defined interest profiles (see Open Items PM-002)
- Advanced personalization/recommendation engine
- GitHub account integration (OAuth-based personalization)
- Trend forecasting (predictive, vs. v1's descriptive trend detection)
- Semantic/intent-based search
- Repository-to-repository comparison

**Excluded by product identity:**
- Social features (comments, following, shared workspaces)
- Team workspaces / multi-user collaboration
- Browser extensions
- Delivery via Teams, Slack, Discord, or RSS

**Scoped down:**
- Bulk repository cloning (selective cloning only, via the GitHub API path)

## Open Items

| ID | Item | Owner | Due |
|----|------|-------|-----|
| PM-001 | Resolve PRD Q2: set numeric success-metric targets after an initial post-launch usage baseline | Maxx | Post-launch |
| PM-002 | Resolve PRD Q3: decide whether personalized discovery enters an early post-MVP phase | Maxx | After v1 ships |
| PM-003 | Define the revisit trigger/threshold for outgrowing single-node Docker Compose (Architecture risk A5) | Maxx | — |
| ~~PM-004~~ | ~~Run the live LM Studio throughput benchmark; confirm model availability; resolve risk A2~~ — **Closed 2026-08-01**: model confirmed (`google/gemma-4-e4b`), throughput measured (2.57-2.82s p95, Pass), risk A2 resolved. See `docs/spikes/f-002-lm-studio-throughput-benchmark.md` §9. | Maxx | Closed |
| ~~PM-005~~ | ~~F-008 (Summarizer) implementation must not reuse the F-002 spike's `max_tokens: 300` setting unmodified — `google/gemma-4-e4b` spends 65-86% of that budget on internal reasoning, truncating output~~ — **Closed 2026-08-01, resolved by model swap, not by adjusting `max_tokens`**: live comparison against 4 alternatives (spike §10) found `llama-3.2-3b-instruct` produces complete, natural-stop output with zero reasoning-token waste and is ~3x faster. ADR-017 supersedes ADR-013. F-008 still shouldn't assume unlimited headroom at `max_tokens: 300` for the new model (165/300 tokens used in testing — comfortable, not unlimited), but there's no known truncation risk requiring special handling. | Maxx | Closed |
| PM-006 | F-010's two new `Repository` columns (`Topics`, `FirstDiscoveredAtUtc`) have no backfill for rows that existed before the migration — they default to `{}` and `-infinity` respectively, so any repo crawled before F-010 shipped has no topics and permanently sorts as "oldest" under Discovery Feed's default Newest sort until it's naturally re-crawled (which repopulates `Topics` but never `FirstDiscoveredAtUtc`, by design — that field is set-once). Low impact at current data volume (re-crawls happen daily per F-006's schedule, so `Topics` self-heals quickly); `FirstDiscoveredAtUtc` does not self-heal and needs an explicit decision (one-time backfill script vs. accept as a permanent v1 wrinkle) before F-011 ships a "Newest" sort a user will actually look at. **Note (2026-08-03, F-012 Finalization): a `20260802091937_BackfillFirstDiscoveredAtUtc` migration now exists in `src/backend/GitCrawler.Api/Data/Migrations/`, predating this session — status not verified as part of F-012's scope; worth confirming and closing this item in a future session rather than carrying it forward indefinitely.** | Maxx | Before F-011 ships |
| PM-007 | `src/frontend`'s devDependency tree has 6 moderate-severity vulnerabilities (`@hono/node-server` <2.0.5 path traversal, GHSA-frvp-7c67-39w9, reachable via `@modelcontextprotocol/sdk` → `@angular/cli` → `angular-eslint`/`@angular-eslint/builder`/`@angular-eslint/schematics`), introduced by commit `8387b4e` ("added playwright, make dev for out of container app development"). Confirmed dev-only (`npm audit --omit=dev` → 0; production dependency tree is clean) and unrelated to any orchestrated feature's own diff. The only fix (`npm audit fix --force`) forces a breaking `@angular/cli` major-version downgrade (22.x → 21.0.4) — needs an explicit human decision on toolchain compatibility risk before it's applied, not something an Integration Agent should force through. | Maxx | — |

## Version History
| Version | Date | Change | Triggered By |
|---------|------|--------|---------------|
| v1 | 2026-07-28 | Initial draft | — |
| v2 | 2026-07-28 | F-003 and F-011 updated for Angular dashboard (was Blazor Server) | Ripple from Architecture v4 |
| v3 | 2026-07-28 | F-004, F-006, and F-014 updated for Hangfire (was Quartz.NET); F-006 now also covers dashboard access control | Ripple from Architecture v5 |
| v4 | 2026-07-28 | F-003 acceptance criteria pinned to .NET 10, plus a package-compatibility check | Ripple from Architecture v6 |
| v5 | 2026-07-28 | F-003 and F-011 updated for Angular Material (ADR-011); F-003 now also pins the Angular version in package.json at scaffolding time | Ripple from Architecture v7 |
| v6 | 2026-07-28 | F-003 acceptance criteria pinned to Angular 22 specifically (was "latest stable") | Ripple from Architecture v8 |
| v7 | 2026-07-28 | Added F-018 (Dashboard UX design brief & Claude Designer handoff, Phase 2, Must); F-011 now depends on F-018 in addition to F-010 | Triage edit |
| v8 | 2026-07-28 | F-002, F-003, F-004, and F-008 updated to pin Gemma 4 E4B (ADR-013) and PostgreSQL 18.4 (ADR-014) | Ripple from Architecture v9 |
| v9 | 2026-07-28 | F-003, F-005 through F-010, F-013, and F-014 updated for Vertical Slice + CQRS via Wolverine (ADR-015), not MediatR | Ripple from Architecture v10 |
| v10 | 2026-07-28 | Status → ACTIVE | Gate approval |
| v11 | 2026-07-31 | Phase 0 complete: F-001, F-002, F-003 → Done. F-002's Acceptance Criteria reworded to explicitly allow "unverifiable in this environment, operator plan provided" as a valid terminal state (matches the Task Packet's original intent, closes a doc-drift finding raised by the Reviewer-Integration gate). Added PM-004 (run the live LM Studio benchmark before F-008) to track the resulting open verification. Phase 0 status → Done. | Orchestrator Finalization, Integration Agent doc-drift finding |
| v12 | 2026-08-01 | LM Studio changed from a Docker Compose container to host-installed (ADR-016) — F-003's AC updated to describe the new `Makefile`-orchestrated topology (Compose for app+postgres, Makefile for LM Studio). F-002's AC updated: model identifier now confirmed live (`google/gemma-4-e4b`) via `lms ls`/`lms load` against the operator's actual install; PM-004 narrowed to just the still-open throughput benchmark. | Operator correction: LM Studio already installed on the target machine, no container needed |
| v13 | 2026-08-01 | F-002's live throughput benchmark executed for real against `google/gemma-4-e4b` — 2.57-2.82s p95 per repo, Pass vs. NFR-001. PM-004 closed. New PM-005 added: F-008 must address a reasoning-token budget-truncation finding from the live run rather than reusing the spike's `max_tokens: 300` setting; F-008's AC updated accordingly. Architecture risk A2 → Resolved (Architecture v12). | F-002 spike executed live per operator request |
| v14 | 2026-08-01 | Summarization model changed from Gemma 4 E4B to Llama 3.2 3B Instruct, following a live comparison against 4 alternatives (spike §10) — ADR-017 (new) supersedes ADR-013. F-002 and F-008 AC updated; PM-005 closed by the model swap (not by the `max_tokens` mitigation it was originally written around). Architecture v13. | Operator: "use llama-3.2-3b-instruct, update docs. update spike and ADRs" |
| v15 | 2026-08-02 | Phase 1 complete: F-004, F-005, F-006, F-007 → Done (each PASS on first Developer/Reviewer attempt). F-007's AC amended mid-flight to add star count as a fifth weighted signal (operator direction, post-signoff) and to note the completed F-006→F-007 `ContinueJobWith` chain. F-004's AC annotated with the Hangfire-schema-separation clarification. F-005's AC annotated confirming the F-001 spike's §6/§7 mitigations were genuinely implemented, not just documented. F-006's AC annotated with the actual one-chain-link v1 state and the dashboard's shared-secret access-control mechanism. Phase 1 status → Done. | Orchestrator Finalization, Integration Agent + Reviewer-Integration both PASS |
| v16 | 2026-08-02 | F-006's dashboard access control removed: the shared-secret `?key=` filter broke the dashboard's own static assets and live stats polling (neither carry the query string forward), so it was dropped entirely rather than special-cased — no auth system exists elsewhere in this single-operator v1 to replace it with. F-006's AC and ADR-009's Consequences updated accordingly. | Operator: "remove the auth for hangfire" |
| v17 | 2026-08-02 | Phase 2 complete: F-008, F-009, F-018 → Done. All three verified against the actual implementation (not just the Orchestrator's self-reported annotations) — annotations checked out accurate. Phase 2 status → Done. | Integration Agent (Phase 2) |
| v18 | 2026-08-02 | F-010 (Web API) → Done, run as a standalone slice of Phase 3 rather than the full phase — F-011 and F-012 remain Planned, Phase 3 itself stays Planned until they complete. F-010's row annotated with implementation detail (8 Wolverine slices, shared filter/sort/paginate helper, two bundled additive schema columns — `Topics`, `FirstDiscoveredAtUtc` — and the Categories/Trending data-model decisions). Both new columns require every pre-existing `Repository` row backfilled (`Topics` defaults to `{}`, `FirstDiscoveredAtUtc` defaults to `-infinity`, meaning existing repos sort as "oldest" under the new Newest sort until backfilled) — not yet backfilled, carried as an open item. New PM-006 added for this. | Orchestrator Finalization (F-010-scoped run), Integration + Reviewer-Integration both PASS |
| v19 | 2026-08-02 | F-018's row updated to record that the design pass (run earlier in this session, before F-010 work started) has been reviewed and its three flagged gaps (filter-panel internals, "Summary pending" placeholder, sign-off on three brief-delegated choices) confirmed resolved — F-018's gating condition on F-011 is now satisfied. This fact existed only in conversation history until now; recording it here so a future session doesn't have to re-derive design-readiness from scratch. | Retroactive documentation of a design-review pass this session already completed, closed as part of the same Finalization pass as v18 |
| v20 | 2026-08-02 | F-011 (Web Dashboard) → Done, run as a standalone slice of Phase 3 (same pattern as F-010's v18 run) — F-012 remains Planned, Phase 3 itself stays Planned until it completes. F-011's row annotated with implementation detail and the one Reviewer FAIL round (960px filter-bar collapse, Categories `mat-card` violation) fixed and re-verified PASS. | Orchestrator Finalization (F-011-scoped run), Developer/Reviewer loop PASS after 2 iterations |
| v21 | 2026-08-02 | F-011's row amended: the Integration pass following v20 found and fixed a genuine pre-ship runtime defect the Developer/Reviewer loop hadn't caught — `FilterSortBar` was missing a `MatInputModule` import, so the Star-range Min/Max facet inputs would have thrown `mat-form-field must contain a MatFormFieldControl` in a live browser. Recorded so the row's "filter/sort controls work end-to-end" claim reflects when it actually became true. | Integration Agent (F-011-scoped run) — Reviewer-Integration caught the Integration Output's Documentation Drift section asserting a quote that didn't actually exist in this file yet; corrected by making the underlying PMBook edit for real rather than just fixing the report |
| v22 | 2026-08-03 | F-012 (Bookmarking) → Done, run as a standalone slice of Phase 3 (same pattern as F-010/F-011's own runs) — Phase 3 now fully complete (F-010, F-011, F-012 all Done), Phase 3 status → Done. F-012's row annotated with implementation detail: the dedicated `/bookmarks` view and live nav entry were the only remaining scope (bookmark toggle + backend CRUD had already shipped inside F-010/F-011). Developer/Reviewer loop PASSed on the first attempt; Integration initially FAILed on a pre-existing, unrelated `npm audit` finding, which Reviewer-Integration correctly pushed back on (round 1 FAIL) for two legitimately in-scope doc-drift fixes left undone and an unverified precedent claim — both addressed in an Integration retry (fixed `docs/test-runbook.md` and `docs/test-cases.md` TC-011-11's staleness, and disproved the "F-011 passed with the same issue" claim via git history: the vulnerable devDependency was introduced by commit `8387b4e`, after F-011's own finalization), reaching PASS on round 2. New PM-007 added for the carried-forward `npm audit` finding (dev-only, pre-existing, requires a human decision on a breaking `@angular/cli` bump). PM-006 noted but not resolved — a backfill migration matching its description now exists but wasn't verified as part of this run's scope. | Orchestrator Finalization (F-012-scoped run), Integration + Reviewer-Integration PASS after 1 retry |
| v23 | 2026-08-03 | Categories tab decommissioned: the standalone Categories view + Category drill-down (`features/categories/`) and their backing `GetCategoryRepositories` endpoint were removed, since Category ≡ `Repository.PrimaryLanguage` and that value was already fully filterable via the existing Language facet on Discovery Feed/Hidden Gems — the tab was a redundant second way to reach the same data, not a distinct capability. `GetCategories` itself is unchanged (still backs the Language filter's option list). F-010's/F-011's rows amended in place rather than reopened, matching this doc's own convention for scope changes to already-Done features. FR-009/PRD US-8 updated to list three views instead of four (PRD v5, Architecture v14). Not run through `orchestrator-development-pattern` — implemented directly via Claude Code at the operator's explicit direction, so no Developer/Reviewer/Integration loop applies here; verified directly instead: backend 116/116, frontend 51/51, both green. | Operator: "make category a filter and get rid of the category tab on UI and under the hood plumbing. This change impacts docs and other supporting elements in the repo, keep an open eye and ensure no stale info is left behind" |
| v24 | 2026-08-03 | Trending tab decommissioned and merged into Hidden Gems: the standalone Trending view (`features/trending/`) and its backing `GetTrending` endpoint were removed entirely (unlike Categories' `GetCategories`, nothing else consumed it). Each Hidden Gems card now shows its own category's trend growth directly (`HiddenGemCardDto.trendGrowth`, computed server-side from `TrendAggregate` using the same current/previous-period formula the old view used client-side), rendered as a chip on `RepositoryCard`. Nav is now Discovery Feed → Hidden Gems → Bookmarks. F-010's/F-011's rows amended in place a second time, same convention as v23. FR-009/PRD US-8 updated to list two views (PRD v6, Architecture v15). Also implemented directly via Claude Code, not orchestrated; verified directly: backend 114/114, frontend 47/47, `npm run lint` clean. | Operator: "merge trending, add the trending score to the repo card on the hidden gems and then remove the trending tab as well" |
| v25 | 2026-08-04 | F-008's row amended: Summarizer prompt changed from a "concise, structured" (headed-sections) output to a plain-text, ~3-sentence output explicitly capped at `Summarization:MaxSummaryLength` characters (default 220, new config) — the structured output's own heading text was eating into the dashboard card's fixed 3-line CSS clamp and crowding out the actual summary content, so the length is now enforced by the prompt instead of by truncating the response. `GenerateSummariesJob` also gained its own standalone hourly recurring trigger (`Hangfire:SummarizationCronSchedule`, new config) alongside its existing chain attachment, so a scored-but-unsummarized backlog no longer waits for the next daily crawl cycle to be picked up. Architecture v19. Implemented directly via Claude Code, not orchestrated (single-slice config/prompt change); verified directly: backend 86/86, `dotnet format --verify-no-changes` clean. **Note found while editing this row**: `docs/handoff.md`'s three most recent entries (Repo card polish, Discovery Feed removal, Bookmarks removal) cite this file at v25/v26/v27 respectively, but this file's own version history had only reached v24 before this edit — a pre-existing drift between the two docs, not introduced by or fixed in this change; flagged here rather than silently renumbered, since resolving it needs to know which of the three uncommitted-at-the-time edits the higher numbers were actually meant to correspond to. | Operator: "we need to check more frequently for the repos that dont have a summary... summary should be x characters long... adjust prompt and ask for 3 liner (x character summary) so we dont have to truncate" |
| v26 | 2026-08-04 | F-008's row amended again: Summarizer now generates two distinct summaries per repo (short for the card, detailed for the click-through dialog) via two separate LM Studio calls, instead of one summary shared by both surfaces. `IRepositorySummarizer` contract changed to return both (`RepositorySummaryResult`); `Summary.Content` split into `ShortContent`/`DetailedContent` via migration `SplitSummaryContentIntoShortAndDetailed`, which deletes every pre-existing Summary row (operator-confirmed - Summary is create-once, so there's no backfill path for the new field otherwise; affected repos get re-picked-up automatically by the existing "no Summary row yet" selection filter). `RepositoryCardDto`/`HiddenGemCardDto` gained `DetailedSummaryContent`; `RepositoryDetailPane` now reads it instead of the short `SummaryContent` it previously shared with the card. Architecture v20. Implemented directly via Claude Code, not orchestrated; verified directly: backend 86/86, frontend 45/45, `dotnet format --verify-no-changes` and `npm run lint` both clean. | Operator: "there should be two kinds of summaries: short that show on the repo card and then the detailed one" |
| v27 | 2026-08-04 | F-008's row amended again: a live `make dev` run against the real backlog (the spot-check v26's row flagged as not yet done) found `LmStudioRepositorySummarizer` sending a repo's full README to the model with no length cap - `openclaw/openclaw`'s 111KB README exceeded the loaded model's 8192-token context window outright (LM Studio's own error: `n_keep: 35489 >= n_ctx: 8192`), so any repo with a large enough README would fail the same way, not just this one. Fixed by truncating `ReadmeContent` to `Summarization:MaxReadmeCharacters` (new config, default 6000) before either prompt is built, and by surfacing LM Studio's actual response body on a non-success status instead of discarding it via a bare `EnsureSuccessStatusCode()` (the fix that made this diagnosable at all, without a manual repro). F-012's row also touched in passing: `BookmarkToggle`'s snack-bar toasts were functionally complete but `styles.scss` never actually defined the `.app-snackbar`/`.app-snackbar-error` panelClass rules a stale comment claimed already existed, so every toast rendered as Material's stock gray bar instead of this design's dark pill - added the missing rules. Implemented directly via Claude Code, not orchestrated (isolated bug fixes to existing Done features); verified directly: backend 86/86, frontend 45/45, `dotnet format --verify-no-changes` and `npm run lint` both clean; the corrected summarizer behavior itself wasn't re-run end-to-end against the live batch job this session. | Operator pasted a live LM Studio 400 error from a `make dev` session; asked separately to confirm bookmark toast styling matched a reference screenshot |
| v28 | 2026-08-04 | F-009's/F-010's rows amended: Hidden Gems' per-card trend growth (`HiddenGemCardDto.TrendGrowth`) is now computed per repository instead of per language/category. It had inherited the standalone Trending view's `TrendAggregate` rollup verbatim when that view was merged into Hidden Gems (v15/Architecture v15), which meant every repository of a given language displayed the exact same growth figure - not each repo's own standing. `GetHiddenGemsQueryHandler` now diffs a repository's own two most-recent `Score` rows (`ComputeScoresCommandHandler` already appends a new row per re-crawl, so no schema change was needed) instead of querying `TrendAggregate` at all for this purpose; falls back to `"{score} current score"` (was `"{avg} avg score"`) when a repo has only ever been scored once. `TrendAggregate`/`AggregateTrendsCommand` themselves are unchanged - still the Language filter's option-list source via `GetCategoriesQuery`. Architecture v22. Implemented directly via Claude Code, not orchestrated; verified directly: backend 85/85 (three `GetHiddenGemsQueryHandlerTests` cases rewritten to score-history fixtures instead of `TrendAggregate` fixtures; one case retired as redundant with the rewritten pair), `dotnet format --verify-no-changes` clean; no frontend logic changed (comments only) | Operator: "Trend is currently calculated per language. I want it to be calculated per repository and then shown. What is currently being shown is the trend aggregate for the topic?" |
| v29 | 2026-08-04 | F-011's row amended: fixed a narrow-viewport (<960px) defect where the repository grid's own "Open on GitHub" links painted on top of the opened filter `mat-sidenav` panel instead of being covered by it (operator screenshot). `.filter-bar__sheet-container` (`.mat-drawer-container`) ships Angular Material's own default `z-index: 1` - this app's existing override already raised the sidenav/backdrop *children* to `z-index: 20`, but not the container element itself, leaving the container's own subtree only at `z-index: 1` when compared against sibling page content elsewhere in the DOM. Added `z-index: 20; isolation: isolate;` directly on the container so the whole subtree is unambiguously above the grid, matching its children. Also corrected a stale comment in the same file still describing app.scss's toolbar as `z-index: 10` and referencing a `.app-bottom-nav` class removed earlier in this session. Implemented directly via Claude Code, not orchestrated; verified: frontend 45/45, `npm run lint` clean, and operator-confirmed live against the actual narrow-viewport render ("it worked") - no browser tool was available to re-screenshot it directly, so this confirmation came from the operator, not an automated check | Operator screenshot: filter sidenav open at narrow width, with repo-card "GitHub" links visible through/over the panel |
| v30 | 2026-08-04 | Full documentation sync pass across every governed doc, prompted by the operator asking to "update all documents for whatever has been implemented so far" once the narrow-viewport fix (v29) was confirmed working. `docs/prd.md` (v8): US-8 corrected — it described the trend chip as "each hidden gem's own category trend," which was never accurate for the per-repository computation v28 shipped; reworded. `docs/architecture.md` (v22): no content change needed beyond its own header (see below), already updated for the per-repo trend in v22/for the README cap in v21. `docs/test-cases.md` (v14) and `docs/test-runbook.md`: both had fallen behind several rounds of direct UI/backend changes made earlier the same day — corrected stale references to the card's own "Why this score?" panel and trend chip (both removed, consolidated into the click-through detail dialog), the per-*category* `TrendAggregate`-based trend computation (now per-repository, TC-010-11/TC-011-13 rewritten), the right-side `mat-drawer` detail pane (now a centered `MatDialog`, TC-011-14/15 updated), a bottom pill nav that no longer exists (the primary nav was removed entirely, not just narrowed, once Hidden Gems became the dashboard's sole page), and the renamed `Summary.Content` → `ShortContent`/`DetailedContent` columns; added new coverage for the README-length cap (TC-008-09), the paginator's page-size dropdown (TC-011-16), and a regression check for the narrow-viewport fix itself (TC-011-09 step 4). **Separately found and fixed while doing this pass**: `docs/prd.md`, `docs/architecture.md`, and this file's own header `Version` markers had each silently fallen behind their own changelog tables (e.g. Architecture's header still read v18 while its own table already reached v22) — a drift introduced across this session's many rapid-fire single-topic edits, where the changelog row was added but the header line above it wasn't touched in the same edit. Fixed in all three files, and each file's cross-references to the others' versions (e.g. this file's "PRD: docs/prd.md (built against vN)") updated to match. Not run through `orchestrator-development-pattern` (a documentation-only pass, no code changed); verified by reading every corrected claim back against the actual current source (component templates, DTOs, migration files) rather than assuming the correction was right on the first pass. | Operator: "now update all documents for whatever has been implemented so far" |
| v31 | 2026-08-04 | Dead-code audit after the per-repository trend change (v28): operator asked to clean up whatever endpoints/services/DB tables the change had made unnecessary. Audited every endpoint, `DbSet`, and backend/frontend service against actual live consumers rather than assuming — `TrendAggregate`/`AggregateTrendsCommand`/`GetCategoriesQuery`/`/api/categories` all confirmed still load-bearing (the Language filter's dropdown options), so none of that was touched; F-009's/F-010's rows are unaffected. The one genuine find: 4 of 13 registered Lucide-style icons (`search`, `sliders-horizontal`, `home`, `gem`) had no template or code reference anywhere in the frontend — leftovers from UI already removed in earlier rounds this session (the old bottom nav's home icon, an old mobile filter-trigger icon, an icon-based search trigger later replaced by a plain "Search (v2)" text pill, and a Hidden-Gems-tab-era icon) — removed from `icon-registry.service.ts`. Implemented directly via Claude Code, not orchestrated; verified: frontend 45/45, `npm run lint` clean. | Operator: "time to clean up whatever is not needed e.g. endpoints, services, db tables" |
| v32 | 2026-08-04 | F-010's row amended, superseding v31's "TrendAggregate... confirmed still load-bearing" note: operator followed up asking whether the Language filter really needed the whole `TrendAggregate` table for that, prompting a closer look than v31's pass took. It didn't — `FacetOptionsService` (the filter's sole consumer) only ever read the `category` string off each `CategoryDto`, discarding `RepositoryCount`/`AverageScore`/`PeriodStart`/`PeriodEnd` on every response. `GetCategoriesQueryHandler` now queries `Repository.PrimaryLanguage` directly (`Scores.Any()` filter, matching `GetHiddenGemsQueryHandler`'s own eligibility) instead of `TrendAggregate`; `CategoryDto` simplified to a single `Category` field, mirrored on the frontend. This also fixed a latent gap: the old version required both a `Score` and a `Summary` (F-009's own "scored + summarized" rollup criteria) before a language appeared as a filter option, stricter than Hidden Gems' own Score-only requirement — a newly-scored-but-unsummarized repo's language used to be unfilterable until the next nightly Trend Aggregator run despite the repo already being visible on Hidden Gems. Presented as a 3-way tradeoff (simplify Categories only / remove the Trend Aggregator entirely / leave as-is) since `TrendAggregate`/`AggregateTrendsCommand` (F-009) remain documented as feeding F-013's planned digest — operator chose to simplify Categories only and leave F-009's pipeline running unchanged, reserved for that future use. Architecture v23. Implemented directly via Claude Code, not orchestrated; verified: backend 86/86, frontend 45/45, `dotnet format --verify-no-changes` and `npm run lint` both clean. | Operator: "So for just the language filter we have the whole TrendAggregate table?" |
| v33 | 2026-08-04 | Phase 4 complete: F-013 (Digest Service) and F-014 (Observability) → Done via a full `orchestrator-development-pattern` run. `docs/test-cases.md` (v16) drafted TC-013/TC-014 ahead of implementation (operator chose "draft now" at Step 0.0, since no Phase 4 coverage existed yet). F-013 PASSed on the first Developer/Reviewer attempt. F-014 PASSed after 1 retry — Reviewer round 1 FAILed on two real defects (failure path not logging `RecordsProcessed`; a records-processed heuristic misreporting an entity's `Id` field as a count), both fixed and re-verified. Integration PASS (103/103 backend tests; one test-only bug fixed — `ObservabilityMiddlewareTests.MessageTypeOf` compared against the wrong `Envelope.MessageType` string shape, not a production defect; `dotnet list package --vulnerable` clean). Integration's Documentation Drift Check found and fixed two real gaps: `docs/architecture.md` §3 had no Observability component entry at all, and its Job Scheduler prose implied Digest Service was chain link 5 when it's actually an independent daily `RecurringJob` (Architecture → v24); `docs/test-runbook.md` had zero F-013/F-014 coverage (added). Reviewer-Integration PASS. graphify re-run on `src/backend` (first run at that directory scope — no prior `graph.json` existed there despite leftover AST cache; 1102 nodes/1635 edges/75 communities). | Orchestrator Finalization, Integration + Reviewer-Integration both PASS |
